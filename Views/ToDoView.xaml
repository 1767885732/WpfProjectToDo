<UserControl x:Class="WpfProjectToDo.Views.ToDoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfProjectToDo.Views"
             xmlns:cv="clr-namespace:WpfProjectToDo.Common.Converters"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <cv:IntToVisibilityConvert x:Key="IntToVisibilityConvert"/>
    </UserControl.Resources>
    <materialDesign:DialogHost>
        <materialDesign:DrawerHost IsRightDrawerOpen="{Binding IsRightDrawerOpen}">
            <materialDesign:DrawerHost.RightDrawerContent>
                <DockPanel Width="300" LastChildFill="False">
                    <TextBlock Padding="20,10" Text="添加待办" FontSize="20" FontWeight="Bold" DockPanel.Dock="Top"/>

                    <StackPanel Margin="20" Orientation="Horizontal" DockPanel.Dock="Top">
                        <TextBlock Text="状态: " VerticalAlignment="Center"/>
                        <ComboBox SelectedIndex="{Binding CurrentDto.Status}">
                            <ComboBoxItem Content="待办"></ComboBoxItem>
                            <ComboBoxItem Content="已完成"></ComboBoxItem>
                        </ComboBox>

                    </StackPanel>
                    <TextBox Text="{Binding CurrentDto.Title}" Margin="20,0" materialDesign:HintAssist.Hint="请输入待办概要" DockPanel.Dock="Top"/>
                    <TextBox Text="{Binding CurrentDto.Content}" Margin="20" MinHeight="100" materialDesign:HintAssist.Hint="请输入待办内容" DockPanel.Dock="Top"/>
                    <Button Command="{Binding ExecutCommand}" CommandParameter="保存" Margin="20,0" Content="添加到待办" DockPanel.Dock="Top"/>
                </DockPanel>
            </materialDesign:DrawerHost.RightDrawerContent>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <StackPanel Margin="15,0,0,0" Orientation="Horizontal">
                    <TextBox Text="{Binding Search,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" materialDesign:HintAssist.Hint="查找待办事项..." materialDesign:TextFieldAssist.HasClearButton="True" Width="250">
                        <TextBox.InputBindings>
                            <KeyBinding Key=" Enter" Command="{Binding ExecutCommand}" CommandParameter="查询"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    <TextBlock Text="筛选：" Margin="10,0" VerticalAlignment="Center"/>
                    <ComboBox SelectedIndex="{Binding SelectedIndex}">
                        <ComboBoxItem Content="全部"></ComboBoxItem>
                        <ComboBoxItem Content="待办"></ComboBoxItem>
                        <ComboBoxItem Content="已完成"></ComboBoxItem>
                    </ComboBox>

                </StackPanel>
                <Button HorizontalAlignment="Right" Content="+ 添加待办" Margin="10,5" Command="{Binding ExecutCommand}" CommandParameter="新增"/>

                <StackPanel Grid.Row="1" VerticalAlignment="Center" Visibility="{Binding ToDoDtos.Count,Converter={StaticResource IntToVisibilityConvert}}">
                    <Image Source="/Images/empty.png" />
                    <TextBlock Margin="0,10" FontSize="18" HorizontalAlignment="Center" Text="尝试添加一些待办事项，以便在此处查看它们。"/>
                </StackPanel>
                
                <ScrollViewer Grid.Row="1" >
                    <ItemsControl ItemsSource="{Binding ToDoDtos}" HorizontalAlignment="Center">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <materialDesign:TransitioningContent OpeningEffect="{materialDesign:TransitionEffect Kind=ExpandIn}">
                                    <Grid MinWidth="220" MinHeight="180" MaxHeight="250" Margin="8" >
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="MouseLeftButtonUp">
                                                <i:InvokeCommandAction 
                                                    CommandParameter="{Binding}"
                                                    Command="{Binding DataContext.SelectedCommand, RelativeSource={ RelativeSource Mode=FindAncestor,AncestorType=ItemsControl}}"/>
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>

                                        <materialDesign:PopupBox HorizontalAlignment="Right" Panel.ZIndex="1">
                                            <Button Content="删除"  CommandParameter="{Binding}"
                                                    Command="{Binding DataContext.DeleteCommand, RelativeSource={ RelativeSource Mode=FindAncestor,AncestorType=ItemsControl}}"/>
 
                                        </materialDesign:PopupBox>

                                        <Border CornerRadius="3" Grid.RowSpan="2" >
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="0">
                                                            <Setter Property="Background" Value="LightBlue"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="1">
                                                            <Setter Property="Background" Value="DarkGray"/>
                                                        </DataTrigger>
                                                        
                                                        
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                        </Border>

                                        <TextBlock Padding="10 5" Text="{Binding Title}"/>
                                        <TextBlock Padding="10 5" Text="{Binding Content}" Grid.Row="1"/>

                                        <Canvas Grid.RowSpan="2" ClipToBounds="True">
                                            <Border CornerRadius="100" Canvas.Top="10" Canvas.Right="-50" Width="120" Height="120" Background="#FFFFFF" Opacity="0.1"/>
                                            <Border CornerRadius="100" Canvas.Top="80" Canvas.Right="-30" Width="120" Height="120" Background="#FFFFFF" Opacity="0.1"/>
                                        </Canvas>
                                    </Grid>
                                </materialDesign:TransitioningContent>

                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
 
                </ScrollViewer>

            </Grid>
        </materialDesign:DrawerHost>

    </materialDesign:DialogHost>

</UserControl>
